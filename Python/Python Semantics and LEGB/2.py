# ------------------------------------------------------------
# 2. Name Binding
# ------------------------------------------------------------
# Name binding means attaching a name to an object.
# Binding happens when we assign a value.
# Rebinding changes what object the name points to.
# It does NOT change the old object.

x = 10
x = 20          # x now points to 20, not 10
